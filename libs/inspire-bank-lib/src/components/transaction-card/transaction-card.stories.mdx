import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { TransactionCard } from './transaction-card';

<Meta title="Inspire Bank/Transaction Card" component={TransactionCard} />

export const Template = (args) => <TransactionCard {...args} />;

# Transaction Card

Description Here

<Canvas>
  <Story
    name="Primary"
    args={{ balance: 1000, transaction: 100 }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
